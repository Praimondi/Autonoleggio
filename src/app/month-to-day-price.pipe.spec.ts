import { MonthToDayPricePipe } from './month-to-day-price.pipe';

describe('MonthToDayPricePipe', () => {
  it('create an instance', () => {
    const pipe = new MonthToDayPricePipe();
    expect(pipe).toBeTruthy();
  });
});
